# Legacy AI Blog Archive

This directory preserves the raw JSON outputs from the retired pre-v3 AI blog system.

These files are no longer part of the canonical live article pipeline.
They are kept only so existing slugs can resolve through the new archive compatibility layer while the active editorial system uses validated MDX files in `content/blog/`.
